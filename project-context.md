# This is the target-project context instance/template.
# Schema authority is `references/contracts.md`.
# If fields change, bump `schema_version` and update every agent that reads project context.

schema_version: "1.1"

tech_stack:
  language: "YOUR_PROJECT_LANGUAGE"
  backend_framework: "YOUR_BACKEND_FRAMEWORK_OR_NONE"
  frontend_framework: "YOUR_FRONTEND_FRAMEWORK_OR_NONE"
  database: "YOUR_DATABASE_OR_DATA_STORE"
  migration_tool: "YOUR_SCHEMA_MIGRATION_TOOL_OR_NONE"
  package_manager:
    backend: "YOUR_BACKEND_PACKAGE_MANAGER_OR_NONE"
    frontend: "YOUR_FRONTEND_PACKAGE_MANAGER_OR_NONE"

conventions:
  naming:
    api_path_style: "YOUR_API_PATH_NAMING_STYLE"
    db_object_style: "YOUR_DATABASE_OBJECT_NAMING_STYLE"
    component_style: "YOUR_COMPONENT_NAMING_STYLE"
    error_code_style: "YOUR_ERROR_CODE_NAMING_STYLE"
  api:
    version_prefix: "YOUR_API_VERSION_PREFIX"
    contract_source: "YOUR_API_CONTRACT_SOURCE"
  commit:
    format: "YOUR_COMMIT_MESSAGE_FORMAT"

toolchain:
  lint:
    backend: ["YOUR_BACKEND_LINT_COMMAND_OR_TOOL"]
    frontend: ["YOUR_FRONTEND_LINT_COMMAND_OR_TOOL"]
  test:
    backend: ["YOUR_BACKEND_TEST_COMMAND_OR_TOOL"]
    frontend: ["YOUR_FRONTEND_TEST_COMMAND_OR_TOOL"]
  build:
    backend: ["YOUR_BACKEND_BUILD_COMMAND_OR_TOOL"]
    frontend: ["YOUR_FRONTEND_BUILD_COMMAND_OR_TOOL"]
  typecheck:
    backend: ["YOUR_BACKEND_TYPECHECK_COMMAND_OR_TOOL"]
    frontend: ["YOUR_FRONTEND_TYPECHECK_COMMAND_OR_TOOL"]
  migrate: ["YOUR_MIGRATION_COMMAND_OR_TOOL"]

ci:
  platform: "YOUR_CI_PLATFORM_OR_NONE"
  pipeline_path: "YOUR_CI_PIPELINE_PATH_OR_NONE"

repo:
  layout:
    api: "YOUR_API_OR_CONTRACT_DIRECTORY"
    backend: "YOUR_BACKEND_SOURCE_DIRECTORY"
    frontend: "YOUR_FRONTEND_SOURCE_DIRECTORY"
    tests: "YOUR_TEST_DIRECTORY"
    migrations: "YOUR_MIGRATION_DIRECTORY"
  api_contract_paths: ["YOUR_API_CONTRACT_PATH"]
  migration_paths: ["YOUR_MIGRATION_PATH"]
  test_paths: ["YOUR_TEST_PATH"]
  generated_code_policy: "YOUR_GENERATED_CODE_POLICY"

runtime:
  auth_model: "YOUR_AUTH_MODEL"
  tenancy_model: "YOUR_TENANCY_MODEL_OR_NONE"
  environment_policy: "YOUR_ENVIRONMENT_POLICY"
  deployment_target: "YOUR_DEPLOYMENT_TARGET"
  observability: "YOUR_LOGGING_TRACING_METRICS_POLICY"
  feature_flag_policy: "YOUR_FEATURE_FLAG_POLICY_OR_NONE"

quality_gates:
  required_before_summary:
    - "schema validation"
    - "blocking issues resolved or accepted"
    - "qa/frontend integration-check when both are activated"

issues: []

# Manual Supplements Area (recommended to be placed outside YAML; here written as YAML comments to avoid affecting parsing):
# - Directory tree: Can paste the directory tree generated by architect-agent at the end of the document;
# - ADR / Change Log: Can be placed in the Markdown description area, but do not introduce new "peer YAML field names".
